struct Empty:

end

struct Foo:
    x i32
    y string
end

struct Boo is Edible, Iterable:
    fun oof(x i32) string:
        64 + 32
        var e = 20
    end
end

struct Moo is Callable:
    num f64
    food string

    assoc shoe():
        var s = "eat"
    end

    fun ooze() string:
        beef + 32; foo
        [4.45, 5]()
    end
end
