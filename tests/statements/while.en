while true == 32.5:
    bar[0] = 42
    [1, 2, 3, 4, 5][0]; bar = bar(1, 2, 3)
    egg + 31
    
    if egg + 40 / 2 == nice:
        print("egg" + 40)
    else:
        var c = meg[45]
        [0, 1, 2]; 40
    end

    break; continue

    var mesh = egg(1, 2, 4, "e")
end
